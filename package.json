{
  "name": "happy-days-new",
  "version": "1.0.0",
  "description": "create web happy days for happy event",
  "main": "index.js",
  "scripts": {
    "build:css": "npx tailwindcss-cli@latest build ./src/styles.css -c ./tailwind.config.js -o ./dist/css/styles.css",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "hafizhmaulanay",
  "license": "ISC",
  "devDependencies": {
    "autoprefixer": "^10.2.5",
    "postcss": "^8.3.0",
    "tailwindcss": "^2.1.2"
  }
}
